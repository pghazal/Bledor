<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <meta name="author" content="Pierre Ghazal">
    <link rel="icon" href="favicon.ico">
    <title>{% block title %}Blédor{% endblock %}</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">

    {% block stylesheets %}
        <link href="{{ asset('css/my-style.css') }}" rel="stylesheet"/>
    {% endblock %}
</head>

<body>
<div class="container">
    <div id="header" class="jumbotron">
        <h1>Blédor</h1>
        <p>
            Pour faciliter vos commandes.
        </p>
    </div>

    {% include '::topnav.html.twig' %}

    <div class="row">
        <div id="menu" class="col-md-3">
            <h3>Menu</h3>
            <ul class="nav nav-pills nav-stacked">
                <li><a href="{{ path('pg_platform_homepage') }}">Accueil</a></li>
                <li><a href="{{ path('pg_platform_products') }}">Nos produits</a></li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('pg_platform_products_add') }}">Ajouter un produit</a></li>
                {% endif %}
                {% if is_granted("ROLE_CLIENT") %}
                    <li><a href="{{ path('pg_platform_operations_add') }}">Passer une commande</a></li>
                {% endif %}
            </ul>
        </div>

        <div id="content">
            {% block content %}
            {% endblock %}
        </div>
    </div>
    <hr>

    <footer>
        <p>Blédor © {{ 'now'|date('Y') }}</p>
    </footer>
</div>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
{% block javascripts %}{% endblock %}
</body>
</html>
