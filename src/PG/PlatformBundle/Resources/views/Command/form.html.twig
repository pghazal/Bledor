{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
<div class="well">
    {{ form_errors(form) }}

    <div class="datepicker input-group date col-sm-3" data-provide="datepicker">
        {{ form_widget(form.date) }}
        <div class="input-group-addon">
            <span class="fa fa-calendar"></span>
        </div>
    </div>

    <table style="width: 100%; " id="command-products-list" class="command-products-list">
        <tr>
            <th>Produit</th>
            <th>Quantité</th>
        </tr>
    </table>

    {{ form_widget(form.submit, {'attr': {'class': 'btn btn-primary'}, 'label': 'pg.validate'|trans}) }}

    {{ form_widget(form.cancel, {'attr': {'class': 'btn btn-primary'}, 'label': 'form.cancel'|trans}) }}
</div>
{{ form_end(form) }}